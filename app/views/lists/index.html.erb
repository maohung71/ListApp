<div class="bigcircle"></div>
<div class="listscontainer">
    <% @lists.each do |l| %>
      <div class="row collection">
        <div class="col-md-3 collectPhoto">image goes here</div>
        <div class="col-md-9">
         <ul>
          <li class ='list'>
            <h2><%= link_to l.name, list_path(l) %></h2>
            <p><%= link_to l.description, list_path(l) %></p>
              <div class ='btn'>
                <p><%= link_to 'Edit', edit_list_path(l) %></p>
                <p><%= link_to 'Delete', l, method: :delete, data: { confirm: 'Are you sure?' }  %></p>
              </div>
          </li>
          </ul>
        </div>
      </div>
    <% end %>
</div>


<h1>LeVEL 1 ALL COLLECTION</h1>
<h1>Lists#index</h1>


